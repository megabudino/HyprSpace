## Codebase Patterns
- AxWindowKindTest expects `./axDumps` to exist when running `./run-tests.sh`.
- Config tests read `docs/config-examples/i3-like-config-example.toml` during `./run-tests.sh`.
- Tiling windows can be identified by a parent `cases` value of `.tilingContainer`.
- Use `getChildParentRelation(child:parent:)` when you need to distinguish tiling windows from popup/dialog windows.

## 2026-01-22 15:05 - US-001
- Extracted `computeMonitorToOptimalHideCorner` helper and reused it in layout.
- Added missing test fixtures for ax dumps and i3-like config parsing.
- Updated ConfigTest expectation for invalid `layout h_tiles` callback errors.
- Files changed: `Sources/AppBundle/layout/refresh.swift`, `Sources/AppBundleTests/config/ConfigTest.swift`, `docs/config-examples/i3-like-config-example.toml`, `axDumps/.gitkeep`, `tasks/prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: `layoutWorkspaces()` uses monitor top-left corners as map keys for per-monitor data.
  - Gotchas encountered: `./run-tests.sh` requires `axDumps` and `docs/config-examples/i3-like-config-example.toml` to exist.
  - Useful context: `OptimalHideCorner` and layout logic live in `Sources/AppBundle/layout/refresh.swift`.
---

## 2026-01-22 15:04 - US-002
- Hid newly registered tiling windows offscreen on visible workspaces before callbacks run.
- Used `computeMonitorToOptimalHideCorner` to pick the hide corner per monitor.
- Files changed: `Sources/AppBundle/tree/MacWindow.swift`, `tasks/prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: new tiling windows can be hidden immediately after binding in `MacWindow.getOrRegister`.
  - Gotchas encountered: none.
  - Useful context: the optimal hide corner map expects monitor top-left corner keys.
---

## 2026-01-22 15:05 - US-003
- Ensured new-window hiding only runs for tiling parent relations, not popup or dialog windows.
- Files changed: `Sources/AppBundle/tree/MacWindow.swift`, `tasks/prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: `getChildParentRelation(child:parent:)` is the safest way to check tiling vs popup/dialog.
  - Gotchas encountered: none.
  - Useful context: dialog windows bind directly to `Workspace`, popup windows bind to `macosPopupWindowsContainer`.
---

## 2026-01-22 15:07 - US-004
- Verified `MacWindow.getOrRegister` already guards `hideInCorner` with `workspace.isVisible` to avoid double-hiding.
- Files changed: `tasks/prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Patterns discovered: visibility checks for new-window hiding live alongside the initial tiling registration logic.
  - Gotchas encountered: none.
  - Useful context: no extra code changes were needed for this story beyond confirming the existing guard.
---
